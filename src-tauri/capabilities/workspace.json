{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace",
  "description": "Folder picking, which only the Workspace offers",
  "windows": ["workspace"],
  "permissions": [
    "dialog:allow-open"
  ]
}
